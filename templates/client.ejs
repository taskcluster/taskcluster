// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

import Client from '../Client';

export default class <%= name %> extends Client {
  constructor() {
    super({
      baseUrl: '<%= client.reference.baseUrl %>',
      exchangePrefix: '<%= client.reference.exchangePrefix %>'
    });
  }<% client.reference.entries.filter(({ type }) => type === 'function').forEach(entry => { %>
<% entry.description.split('\n').filter(Boolean).forEach(line => { %>
  <%- `// ${line}` %><% }) %>
  <%= entry.name %>(...args) {
    const entry = <%- stringify(entry) %>;

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }<% }) %><% client.reference.entries.filter(({ type }) => type === 'topic-exchange').forEach(entry => { %>
<% entry.description.split('\n').filter(Boolean).forEach(line => { %>
  <%- `// ${line}` %><% }) %>
  <%= entry.name %>(routingKeyPattern) {
    const entry = <%- stringify(entry) %>;

    return this.normalizeRoutingKeyPattern(entry, routingKeyPattern);
  }<% }) %>
}
