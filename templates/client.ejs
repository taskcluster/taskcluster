// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

import Client from '../Client';

export default class <%= name %> extends Client {
  constructor(options = {}) {
    super({
      baseUrl: '<%= baseUrl %>',
      exchangePrefix: '<%= exchangePrefix %>',
      ...options
    });
    <% methods.forEach(entry => { %>
    this.<%= entry.name %>.entry = <%- stringify(omit(entry, ['description', 'title'])) %>;<% }) %>
  }<% methods.forEach(entry => { %>
<% entry.description.split('\n').filter(Boolean).forEach(line => { %>
  <%- `// ${line}` %><% }) %>
  <%= entry.name %>(...args) {
    this.validate(this.<%= entry.name %>.entry, args);
    return this.request(this.<%= entry.name %>.entry, args);
  }<% }) %><% topics.forEach(entry => { %>
<% entry.description.split('\n').filter(Boolean).forEach(line => { %>
  <%- `// ${line}` %><% }) %>
  <%= entry.name %>(pattern) {
    const entry = <%- stringify(omit(entry, ['description', 'title'])) %>;

    return this.normalizePattern(entry, pattern);
  }<% }) %>
}
