const simpleEstimate = ({
  pendingTasks,
  pendingCapacity,
  runningCapacity,
  minCapacity,
  maxCapacity,
}) => {
  return maxCapacity; // TODO: Actually do something here
};

exports = {
  simpleEstimate,
};
