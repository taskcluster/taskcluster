subscription TasksSubscriptions($taskGroupId: ID!, $subscriptions: [TaskSubscriptions]!){
  tasksSubscriptions(taskGroupId: $taskGroupId, subscriptions: $subscriptions){
    taskId,
    taskGroupId
  }
}