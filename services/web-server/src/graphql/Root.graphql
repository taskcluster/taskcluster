scalar Date
scalar Time
scalar DateTime
scalar JSON

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

input PageConnection {
  limit: Int
  startCursor: String
  endCursor: String
}

# Every new edge implementation needs to declare its Node type here :(
union Node = Artifact | Task | Provisioner | WorkerType | Worker | WorkerCompact | Namespace | IndexedTask | CachePurge

interface Edge {
  cursor: String
  node: Node
}

interface Connection {
  pageInfo: PageInfo
  edges: [Edge]
}

type Query {
  root: Boolean
}

type Mutation {
  root: Boolean
}

schema {
  query: Query
  mutation: Mutation
}
