---
version: 1
tasks:
  - payload:
      image: "quay.io/mrrrgn/ubuntu-build:0.0.2"
      command:
        - "/bin/bash"
        - "-c"
        - "git clone $GITHUB_HEAD_REPO_URL repo && cd repo && git checkout $GITHUB_HEAD_BRANCH && npm install . && npm test"
