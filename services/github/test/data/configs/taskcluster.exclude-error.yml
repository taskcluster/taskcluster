version: 0
metadata:
  name: "name"
  description: "description"
  owner: "test@test.com"
  source: "http://mrrrgn.com"
tasks:
  - provisionerId: aprovisioner
    workerType: worker
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - master
        excludeBranches:
          - foobar
          - master

    payload:
      image: "ubuntu:latest"
      command:
        - test
    metadata:
      name: "name"
      description: "description"
      owner: "test@test.com"
      source: "http://mrrrgn.com"
