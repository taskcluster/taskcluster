// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

import Client from '../Client';

export default class <%= name %> extends Client {
  constructor(options = {}) {
    super({
      serviceName: '<%= serviceName %>',
      serviceVersion: '<%= serviceVersion %>',
      exchangePrefix: '<%= exchangePrefix %>',
      ...options
    });<% methods.forEach(entry => { %>
    this.<%= entry.name %>.entry = <%- stringify(omit(entry, ['description', 'title'])) %>; // eslint-disable-line<% }) %>
  }<% methods.forEach(entry => { %>
  <%- '/* eslint-disable max-len */' %><% entry.description.split('\n').filter(Boolean).forEach(line => { %>
  <%- `// ${line.trimRight()}` %><% }) %>
  <%- '/* eslint-enable max-len */' %>
  <%= entry.name %>(...args) {
    this.validate(this.<%= entry.name %>.entry, args);
    return this.request(this.<%= entry.name %>.entry, args);
  }<% }) %><% topics.forEach(entry => { %>
  <%- '/* eslint-disable max-len */' %><% entry.description.split('\n').filter(Boolean).forEach(line => { %>
  <%- `// ${line.trimRight()}` %><% }) %>
  <%- '/* eslint-enable max-len */' %>
  <%= entry.name %>(pattern) {
    const entry = <%- stringify(omit(entry, ['description', 'title'])) %>; // eslint-disable-line

    return this.normalizePattern(entry, pattern);
  }<% }) %>
}
