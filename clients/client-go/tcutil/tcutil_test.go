package tcutil

// Note, there are already tests that call tcutil.PublishTask(...) in
// ../integrationtest and in github.com/taskcluster/taskcluster-proxy package,
// therefore any test of tcutil.PublishTask here would probably be overkill.
