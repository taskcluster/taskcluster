query Hooks($hookGroupId: ID) {
  hookGroups(hookGroupId: $hookGroupId) {
    hookGroupId
    hooks {
      hookId
      hookGroupId
      lastFire {
        taskId
        taskCreateTime
        result
        taskState
        error
      }
      bindings {
        exchange
      }
      schedule
    }
  }
}
