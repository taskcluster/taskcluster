query Hooks {
  hookGroups {
    hookGroupId
    hooks {
      hookId
      hookGroupId
      lastFire {
        taskId
        taskCreateTime
        result
        taskState
        error
      }
      bindings {
        exchange
      }
      schedule
    }
  }
}
