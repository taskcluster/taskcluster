mutation PurgeCache($provisionerId: String!, $workerType: ID!, $payload: PurgeCacheInput) {
  purgeCache(provisionerId: $provisionerId, workerType: $workerType, payload: $payload) {
    provisionerId
  }
}
