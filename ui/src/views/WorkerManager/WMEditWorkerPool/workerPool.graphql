query WorkerPool($workerPoolId: String!) {
    WorkerPool(workerPoolId: $workerPoolId) {
        workerPoolId,
        description,
        owner,
        emailOnError,
        providerId,
        config,
        currentCapacity,
        pendingTasks
    }
}
