query workerPools {
  WorkerManagerWorkerPoolSummaries {
    workerPoolId
    providerId
    description
    owner
    config
    emailOnError
    pendingTasks #this value comes from a separate request to queue
  }
}
