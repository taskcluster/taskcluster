query workerTypes {
  WMWorkerTypeSummaries {
    name
    pendingCapacity
    runningCapacity
    pendingTasks
    lastActive
    lastResolved
    failedNumber
    exceptionNumber
    unscheduledNumber
    provider
  }
}