query WorkerManagerErrors($workerPoolId: String, $errorsConnection: PageConnection, $launchConfigId: String, $from: String, $to: String) {
  WorkerManagerErrors(workerPoolId: $workerPoolId, launchConfigId: $launchConfigId, connection: $errorsConnection) {
    pageInfo {
      hasNextPage
      hasPreviousPage
      cursor
      previousCursor
      nextCursor
    }
    edges {
      node {
        errorId
        title
        description
        reported
        extra
        launchConfigId
      }
    }
  }
  WorkerManagerErrorsStats(workerPoolId: $workerPoolId, from: $from, to: $to) {
    totals {
      total
      daily
      hourly
      title
      code
      launchConfig
    }
  }
}
