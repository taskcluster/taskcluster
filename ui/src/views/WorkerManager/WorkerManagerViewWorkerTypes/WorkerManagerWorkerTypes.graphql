query workerTypes {
  WorkerManagerWorkerTypeSummaries {
    name
    pendingCapacity
    runningCapacity
    pendingTasks
    lastActive
    lastResolved
    failedNumber
    exceptionNumber
    unscheduledNumber
    provider
  }
}
