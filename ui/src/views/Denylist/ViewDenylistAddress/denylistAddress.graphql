query DenylistedNotifications($searchTerm: String) {
  listDenylistAddresses(searchTerm: $searchTerm) {
    edges {
      node {
        notificationAddress
        notificationType
      }
    }
  }
}
