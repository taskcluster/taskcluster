query ViewProvisioners {
  provisioners {
    edges {
      node {
        provisionerId
        stability
        description
        expires
        lastDateActive
        actions {
          name
          title
          description
          context
          url
        }
      }
    }
  }
}
