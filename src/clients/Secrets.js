// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

import Client from '../Client';

export default class Secrets extends Client {
  constructor() {
    super({
      baseUrl: 'https://secrets.taskcluster.net/v1',
      exchangePrefix: ''
    });
  }

  // Set the secret associated with some key.  If the secret already exists, it is
  // updated instead.
  set(...args) {
    const entry = {type:'function',method:'put',route:'/secret/<name>',query:[],args:['name'],name:'set',stability:'stable',title:'Set Secret',description:'Set the secret associated with some key.  If the secret already exists, it is\nupdated instead.',scopes:[['secrets:set:<name>']],input:'http://schemas.taskcluster.net/secrets/v1/secret.json#'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // Delete the secret associated with some key.
  remove(...args) {
    const entry = {type:'function',method:'delete',route:'/secret/<name>',query:[],args:['name'],name:'remove',stability:'stable',title:'Delete Secret',description:'Delete the secret associated with some key.',scopes:[['secrets:set:<name>']]};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // Read the secret associated with some key.  If the secret has recently
  // expired, the response code 410 is returned.  If the caller lacks the
  // scope necessary to get the secret, the call will fail with a 403 code
  // regardless of whether the secret exists.
  get(...args) {
    const entry = {type:'function',method:'get',route:'/secret/<name>',query:[],args:['name'],name:'get',stability:'stable',title:'Read Secret',description:'Read the secret associated with some key.  If the secret has recently\nexpired, the response code 410 is returned.  If the caller lacks the\nscope necessary to get the secret, the call will fail with a 403 code\nregardless of whether the secret exists.',scopes:[['secrets:get:<name>']],output:'http://schemas.taskcluster.net/secrets/v1/secret.json#'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // List the names of all secrets that you would have access to read. In
  // other words, secret name `<X>` will only be returned if a) a secret
  // with name `<X>` exists, and b) you posses the scope `secrets:get:<X>`.
  list(...args) {
    const entry = {type:'function',method:'get',route:'/secrets',query:[],args:[],name:'list',stability:'stable',title:'List Secrets',description:'List the names of all secrets that you would have access to read. In\nother words, secret name `<X>` will only be returned if a) a secret\nwith name `<X>` exists, and b) you posses the scope `secrets:get:<X>`.',output:'http://schemas.taskcluster.net/secrets/v1/secret-list.json#'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // Respond without doing anything.
  // This endpoint is used to check that the service is up.
  ping(...args) {
    const entry = {type:'function',method:'get',route:'/ping',query:[],args:[],name:'ping',stability:'stable',title:'Ping Server',description:'Respond without doing anything.\nThis endpoint is used to check that the service is up.'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }
}
