// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

import Client from '../Client';

export default class SchedulerEvents extends Client {
  constructor() {
    super({
      baseUrl: '',
      exchangePrefix: 'exchange/taskcluster-scheduler/v1/'
    });
  }

  // When a task-graph is submitted it immediately starts running and a
  // message is posted on this exchange to indicate that a task-graph have
  // been submitted.
  taskGraphRunning(routingKeyPattern) {
    const entry = {type:'topic-exchange',exchange:'task-graph-running',name:'taskGraphRunning',title:'Task-Graph Running Message',description:'When a task-graph is submitted it immediately starts running and a\nmessage is posted on this exchange to indicate that a task-graph have\nbeen submitted.',routingKey:[{name:'routingKeyKind',summary:'Identifier for the routing-key kind. This is always `\'primary\'` for the formalized routing key.',constant:'primary',multipleWords:false,required:true},{name:'taskId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'runId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerGroup',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'provisionerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerType',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'schedulerId',summary:'Identifier for the task-graphs scheduler managing the task-graph this message concerns. Usually `task-graph-scheduler` in production.',multipleWords:false,required:true},{name:'taskGraphId',summary:'Identifier for the task-graph this message concerns',multipleWords:false,required:true},{name:'reserved',summary:'Space reserved for future routing-key entries, you should always match this entry with `#`. As automatically done by our tooling, if not specified.',multipleWords:true,required:false}],schema:'http://schemas.taskcluster.net/scheduler/v1/task-graph-running-message.json#'};

    return this.normalizeRoutingKeyPattern(entry, routingKeyPattern);
  }

  // When a task-graph is extended, that is additional tasks is added to the
  // task-graph, a message is posted on this exchange. This is useful if you
  // are monitoring a task-graph and what to track states of the individual
  // tasks in the task-graph.
  taskGraphExtended(routingKeyPattern) {
    const entry = {type:'topic-exchange',exchange:'task-graph-extended',name:'taskGraphExtended',title:'Task-Graph Extended Message',description:'When a task-graph is extended, that is additional tasks is added to the\ntask-graph, a message is posted on this exchange. This is useful if you\nare monitoring a task-graph and what to track states of the individual\ntasks in the task-graph.',routingKey:[{name:'routingKeyKind',summary:'Identifier for the routing-key kind. This is always `\'primary\'` for the formalized routing key.',constant:'primary',multipleWords:false,required:true},{name:'taskId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'runId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerGroup',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'provisionerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerType',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'schedulerId',summary:'Identifier for the task-graphs scheduler managing the task-graph this message concerns. Usually `task-graph-scheduler` in production.',multipleWords:false,required:true},{name:'taskGraphId',summary:'Identifier for the task-graph this message concerns',multipleWords:false,required:true},{name:'reserved',summary:'Space reserved for future routing-key entries, you should always match this entry with `#`. As automatically done by our tooling, if not specified.',multipleWords:true,required:false}],schema:'http://schemas.taskcluster.net/scheduler/v1/task-graph-extended-message.json#'};

    return this.normalizeRoutingKeyPattern(entry, routingKeyPattern);
  }

  // When a task is completed unsuccessfully and all reruns have been
  // attempted, the task-graph will not complete successfully and it's
  // declared to be _blocked_, by some task that consistently completes
  // unsuccessfully.
  // When a task-graph becomes blocked a messages is posted to this exchange.
  // The message features the `taskId` of the task that caused the task-graph
  // to become blocked.
  taskGraphBlocked(routingKeyPattern) {
    const entry = {type:'topic-exchange',exchange:'task-graph-blocked',name:'taskGraphBlocked',title:'Task-Graph Blocked Message',description:'When a task is completed unsuccessfully and all reruns have been\nattempted, the task-graph will not complete successfully and it\'s\ndeclared to be _blocked_, by some task that consistently completes\nunsuccessfully.\n\nWhen a task-graph becomes blocked a messages is posted to this exchange.\nThe message features the `taskId` of the task that caused the task-graph\nto become blocked.',routingKey:[{name:'routingKeyKind',summary:'Identifier for the routing-key kind. This is always `\'primary\'` for the formalized routing key.',constant:'primary',multipleWords:false,required:true},{name:'taskId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'runId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerGroup',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'provisionerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerType',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'schedulerId',summary:'Identifier for the task-graphs scheduler managing the task-graph this message concerns. Usually `task-graph-scheduler` in production.',multipleWords:false,required:true},{name:'taskGraphId',summary:'Identifier for the task-graph this message concerns',multipleWords:false,required:true},{name:'reserved',summary:'Space reserved for future routing-key entries, you should always match this entry with `#`. As automatically done by our tooling, if not specified.',multipleWords:true,required:false}],schema:'http://schemas.taskcluster.net/scheduler/v1/task-graph-blocked-message.json#'};

    return this.normalizeRoutingKeyPattern(entry, routingKeyPattern);
  }

  // When all tasks of a task-graph have completed successfully, the
  // task-graph is declared to be finished, and a message is posted to this
  // exchange.
  taskGraphFinished(routingKeyPattern) {
    const entry = {type:'topic-exchange',exchange:'task-graph-finished',name:'taskGraphFinished',title:'Task-Graph Finished Message',description:'When all tasks of a task-graph have completed successfully, the\ntask-graph is declared to be finished, and a message is posted to this\nexchange.',routingKey:[{name:'routingKeyKind',summary:'Identifier for the routing-key kind. This is always `\'primary\'` for the formalized routing key.',constant:'primary',multipleWords:false,required:true},{name:'taskId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'runId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerGroup',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'provisionerId',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'workerType',summary:'Always takes the value `_`',multipleWords:false,required:false},{name:'schedulerId',summary:'Identifier for the task-graphs scheduler managing the task-graph this message concerns. Usually `task-graph-scheduler` in production.',multipleWords:false,required:true},{name:'taskGraphId',summary:'Identifier for the task-graph this message concerns',multipleWords:false,required:true},{name:'reserved',summary:'Space reserved for future routing-key entries, you should always match this entry with `#`. As automatically done by our tooling, if not specified.',multipleWords:true,required:false}],schema:'http://schemas.taskcluster.net/scheduler/v1/task-graph-finished-message.json#'};

    return this.normalizeRoutingKeyPattern(entry, routingKeyPattern);
  }
}
