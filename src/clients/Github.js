// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

import Client from '../Client';

export default class Github extends Client {
  constructor() {
    super({
      baseUrl: 'https://github.taskcluster.net/v1',
      exchangePrefix: ''
    });
  }

  // Capture a GitHub event and publish it via pulse, if it's a push,
  // release or pull request.
  githubWebHookConsumer(...args) {
    const entry = {type:'function',method:'post',route:'/github',query:[],args:[],name:'githubWebHookConsumer',stability:'experimental',title:'Consume GitHub WebHook',description:'Capture a GitHub event and publish it via pulse, if it\'s a push,\nrelease or pull request.'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // A paginated list of builds that have been run in
  // Taskcluster. Can be filtered on various git-specific
  // fields.
  builds(...args) {
    const entry = {type:'function',method:'get',route:'/builds',query:['continuationToken','limit','organization','repository','sha'],args:[],name:'builds',stability:'experimental',title:'List of Builds',description:'A paginated list of builds that have been run in\nTaskcluster. Can be filtered on various git-specific\nfields.',output:'http://schemas.taskcluster.net/github/v1/build-list.json#'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // Checks the status of the latest build of a given branch
  // and returns corresponding badge svg.
  badge(...args) {
    const entry = {type:'function',method:'get',route:'/repository/<owner>/<repo>/<branch>/badge.svg',query:[],args:['owner','repo','branch'],name:'badge',stability:'experimental',title:'Latest Build Status Badge',description:'Checks the status of the latest build of a given branch\nand returns corresponding badge svg.'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // Returns any repository metadata that is
  // useful within Taskcluster related services.
  repository(...args) {
    const entry = {type:'function',method:'get',route:'/repository/<owner>/<repo>',query:[],args:['owner','repo'],name:'repository',stability:'experimental',title:'Get Repository Info',description:'Returns any repository metadata that is\nuseful within Taskcluster related services.',output:'http://schemas.taskcluster.net/github/v1/repository.json'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // For a given branch of a repository, this will always point
  // to a status page for the most recent task triggered by that
  // branch.
  // Note: This is a redirect rather than a direct link.
  latest(...args) {
    const entry = {type:'function',method:'get',route:'/repository/<owner>/<repo>/<branch>/latest',query:[],args:['owner','repo','branch'],name:'latest',stability:'experimental',title:'Latest Status for Branch',description:'For a given branch of a repository, this will always point\nto a status page for the most recent task triggered by that\nbranch.\n\nNote: This is a redirect rather than a direct link.'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // For a given changeset (SHA) of a repository, this will attach a "commit status"
  // on github. These statuses are links displayed next to each revision.
  // The status is either OK (green check) or FAILURE (red cross), 
  // made of a custom title and link.
  createStatus(...args) {
    const entry = {type:'function',method:'post',route:'/repository/<owner>/<repo>/statuses/<sha>',query:[],args:['owner','repo','sha'],name:'createStatus',stability:'experimental',title:'Post a status against a given changeset',description:'For a given changeset (SHA) of a repository, this will attach a "commit status"\non github. These statuses are links displayed next to each revision.\nThe status is either OK (green check) or FAILURE (red cross), \nmade of a custom title and link.',scopes:[['github:create-status:<owner>/<repo>']],input:'http://schemas.taskcluster.net/github/v1/create-status.json'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // For a given Issue or Pull Request of a repository, this will write a new message.
  createComment(...args) {
    const entry = {type:'function',method:'post',route:'/repository/<owner>/<repo>/issues/<number>/comments',query:[],args:['owner','repo','number'],name:'createComment',stability:'experimental',title:'Post a comment on a given GitHub Issue or Pull Request',description:'For a given Issue or Pull Request of a repository, this will write a new message.',scopes:[['github:create-comment:<owner>/<repo>']],input:'http://schemas.taskcluster.net/github/v1/create-comment.json'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }

  // Respond without doing anything.
  // This endpoint is used to check that the service is up.
  ping(...args) {
    const entry = {type:'function',method:'get',route:'/ping',query:[],args:[],name:'ping',stability:'stable',title:'Ping Server',description:'Respond without doing anything.\nThis endpoint is used to check that the service is up.'};

    this.validateMethod(entry, args);
    return this.request(entry, args);
  }
}
