{
  "statsd": {
    "prefix": "{{statsd.prefix}}",
    "url": "udp://{{statsd.host}}:{{statsd.port}}"
  },

  "taskcluster": {
    "clientId": "{{taskcluster.clientId}}",
    "accessToken": "{{taskcluster.accessToken}}"
  },

  "pulse": {
    "username": "{{pulse.username}}",
    "password": "{{pulse.password}}"
  }
}
