apiVersion: batch/v1
kind: CronJob
metadata:
  name: taskcluster-web-server-cleanup-session-storage
  labels: &ref_0
    app.kubernetes.io/name: taskcluster-web-server
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/component: taskcluster-web-server-cleanup-session-storage
    app.kubernetes.io/part-of: taskcluster
spec:
  concurrencyPolicy: Forbid
  schedule: 5 0 * * *
  jobTemplate:
    metadata:
      labels: *ref_0
    spec:
      activeDeadlineSeconds: 86340
      template:
        metadata:
          labels: *ref_0
        spec:
          restartPolicy: OnFailure
          imagePullSecrets: {{ if .Values.imagePullSecret }}{{ toJson (list (dict "name" .Values.imagePullSecret)) }}{{ else }}[]{{ end }}
          securityContext:
            runAsNonRoot: true
          containers:
            - name: taskcluster-web-server-cleanup-session-storage
              image: '{{ .Values.dockerImage }}'
              imagePullPolicy: Always
              args:
                - web-server/cleanup-session-storage
              securityContext:
                allowPrivilegeEscalation: false
              resources:
                requests:
                  cpu: '{{ .Values.web_server.procs.cleanup_session_storage.cpu }}'
                  memory: '{{ .Values.web_server.procs.cleanup_session_storage.memory }}'
              env:
                - name: TASKCLUSTER_ROOT_URL
                  value: '{{ .Values.rootUrl }}'
                - name: USE_KUBERNETES_DNS_SERVICE_DISCOVERY
                  value: '{{ .Values.useKubernetesDnsServiceDiscovery }}'
                - name: NODE_ENV
                  value: production
              envFrom:
                - secretRef:
                    name: taskcluster-web-server
                - configMapRef:
                    name: taskcluster-web-server
