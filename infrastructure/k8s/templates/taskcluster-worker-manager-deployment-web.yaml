apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskcluster-worker-manager-web
spec:
  selector:
    matchLabels:
      application: taskcluster
      taskcluster-service: taskcluster-worker-manager
      taskcluster-proc: web
  replicas: 1
  template:
    metadata:
      annotations:
        checksum/secret: '{{ include (print $.Template.BasePath "/taskcluster-worker-manager-secret.yaml") . | sha256sum }}'
      labels:
        application: taskcluster
        taskcluster-service: taskcluster-worker-manager
        taskcluster-proc: web
    spec:
      serviceAccountName: taskcluster-worker-manager
      containers:
        - name: taskcluster-worker-manager
          image: '{{ .Values.dockerImage }}'
          imagePullPolicy: Always
          args:
            - worker-manager/web
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
          env:
            - name: PORT
              value: '80'
            - name: TASKCLUSTER_ROOT_URL
              value: '{{ .Values.rootUrl }}'
          envFrom:
            - secretRef:
                name: taskcluster-worker-manager
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /api/worker-manager/v1/ping
              port: 80
            timeoutSeconds: 5
            periodSeconds: 10
            initialDelaySeconds: 3
