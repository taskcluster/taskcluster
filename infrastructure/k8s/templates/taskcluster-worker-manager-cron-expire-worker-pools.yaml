apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: taskcluster-worker-manager-expire-worker-pools
  labels:
    app.kubernetes.io/name: taskcluster-worker-manager
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/component: worker-manager-expire-worker-pools
    app.kubernetes.io/part-of: taskcluster
    app.kubernetes.io/managed-by: helm
spec:
  schedule: 0 1 * * *
  jobTemplate:
    metadata:
      labels:
        app.kubernetes.io/name: taskcluster-worker-manager
        app.kubernetes.io/instance: '{{ .Release.Name }}'
        app.kubernetes.io/component: worker-manager-expire-worker-pools
        app.kubernetes.io/part-of: taskcluster
        app.kubernetes.io/managed-by: helm
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: taskcluster-worker-manager
            app.kubernetes.io/instance: '{{ .Release.Name }}'
            app.kubernetes.io/component: worker-manager-expire-worker-pools
            app.kubernetes.io/part-of: taskcluster
            app.kubernetes.io/managed-by: helm
        spec:
          restartPolicy: OnFailure
          activeDeadlineSeconds: 86400
          containers:
            - name: taskcluster-worker-manager-expire-worker-pools
              image: '{{ .Values.dockerImage }}'
              imagePullPolicy: Always
              args:
                - worker-manager/expire-worker-pools
              resources:
                requests:
                  cpu: 100m
                  memory: 100Mi
              env:
                - name: TASKCLUSTER_ROOT_URL
                  value: '{{ .Values.rootUrl }}'
              envFrom:
                - secretRef:
                    name: taskcluster-worker-manager
