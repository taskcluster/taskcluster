apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: taskcluster-ui-web
spec:
  replicas: 1.0
  selector:
    matchLabels:
      application: taskcluster
      taskcluster-proc: web
      taskcluster-service: taskcluster-ui
  template:
    metadata:
      annotations:
        checksum/secret: '{{ include (print $.Template.BasePath "/taskcluster-ui-secrets.yaml") . | sha256sum }}'
      labels:
        application: taskcluster
        taskcluster-proc: web
        taskcluster-service: taskcluster-ui
    spec:
      containers:
      - args:
        - ui/web
        env:
        - name: PORT
          value: '80'
        - name: TASKCLUSTER_ROOT_URL
          value: '{{ .Values.rootUrl }}'
        envFrom:
        - secretRef:
            name: taskcluster-ui
        image: '{{ .Values.dockerImage }}'
        imagePullPolicy: Always
        name: taskcluster-ui
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 10
          timeoutSeconds: 5
        resources:
          requests:
            cpu: 100m
            memory: 2000Mi
      serviceAccountName: taskcluster-ui
