kind: Service
apiVersion: v1
metadata:
  name: taskcluster-queue
spec:
  type: NodePort
  selector:
    application: taskcluster
    taskcluster-service: taskcluster-queue
    taskcluster-proc: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
