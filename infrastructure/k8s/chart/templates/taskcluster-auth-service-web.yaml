kind: Service
apiVersion: v1
metadata:
  name: taskcluster-auth
spec:
  type: NodePort
  selector:
    application: taskcluster
    taskcluster-service: taskcluster-auth
    taskcluster-proc: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
