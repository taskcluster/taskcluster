kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: taskcluster-login-secrets-read
rules:
  - apiGroups:
      - ''
    resources:
      - secrets/taskcluster-login
    verbs:
      - get
      - watch
      - list
