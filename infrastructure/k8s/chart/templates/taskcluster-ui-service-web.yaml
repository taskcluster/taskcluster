kind: Service
apiVersion: v1
metadata:
  name: taskcluster-ui
spec:
  type: NodePort
  selector:
    application: taskcluster
    taskcluster-service: taskcluster-ui
    taskcluster-proc: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
