apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: taskcluster-web-server-web
spec:
  replicas: 1.0
  selector:
    matchLabels:
      application: taskcluster
      taskcluster-proc: web
      taskcluster-service: taskcluster-web-server
  template:
    metadata:
      annotations:
        checksum/secret: '{{ include (print $.Template.BasePath "/taskcluster-web-server-secrets.yaml") . | sha256sum }}'
      labels:
        application: taskcluster
        taskcluster-proc: web
        taskcluster-service: taskcluster-web-server
    spec:
      containers:
      - args:
        - web-server/web
        env:
        - name: PORT
          value: '80'
        - name: TASKCLUSTER_ROOT_URL
          value: '{{ .Values.rootUrl }}'
        envFrom:
        - secretRef:
            name: taskcluster-web-server
        image: '{{ .Values.dockerImage }}'
        imagePullPolicy: Always
        name: taskcluster-web-server
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /.well-known/apollo/server-health
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 10
          timeoutSeconds: 5
        resources:
          requests:
            cpu: 50m
            memory: 100Mi
      serviceAccountName: taskcluster-web-server
