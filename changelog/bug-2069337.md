audience: users
level: patch
reference: bug 2069337
---
D2G: named/registry docker images are always re-pulled and `docker run` pins them by digest, so a poisoned local tag cannot persist across tasks.
