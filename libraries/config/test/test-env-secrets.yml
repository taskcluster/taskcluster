defaults:
  text:       !env:string:secret    ENV_VARIABLE
  number:     !env:number:secret    ENV_NUMBER
  soTrue:     !env:bool:secret      ENV_TRUE
  empty:      !env:secret           ENV_EMPTY
  optional:   !env:secret:optional  ENV_OPTIONAL
  json:       !env:json:secret      ENV_JSON
  list:       !env:list:secret      ENV_LIST
