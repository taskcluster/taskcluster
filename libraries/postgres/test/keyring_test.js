const Keyring = require('../src/keyring');
const path = require('path');
const assert = require('assert').strict;

suite(path.basename(__filename), function() {
  test('configure with legacy config format', function() {
  });
});

