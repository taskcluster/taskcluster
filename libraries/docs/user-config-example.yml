---
defaults:
  taskcluster:
    # This client should have the following scopes:
    # [ auth:aws-s3:read-write:taskcluster-raw-docs/testing ]
    #
    # The client can be created at:
    # https://tools.taskcluster.net/auth/clients/
    credentials:
      clientId: '...'
      accessToken: '...'
